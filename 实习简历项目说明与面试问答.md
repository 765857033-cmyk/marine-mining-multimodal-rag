# 基于 Agentic RAG 的海洋矿产科研文献智能问答系统

## 1. 项目定位

本项目是一个面向海洋矿产科研论文的智能问答系统，主要解决科研人员阅读大量 PDF 文献时检索效率低、图表信息难以利用、回答缺少来源追溯的问题。

系统支持用户上传海洋矿产相关论文 PDF，自动完成文档解析、文本清洗、图表抽取、父子文档切片、向量化入库、混合检索、Rerank、Agent 路由、答案生成、来源校验和中文回答展示。

简历中可以概括为：

> 基于 Agentic RAG 构建海洋矿产科研文献智能问答系统，支持 Docling/PyMuPDF PDF 解析、多模态图表入库、父子文档切片、Hybrid Retrieval、Rerank、LangGraph 工作流、LLM Router、上下文记忆和来源追溯，提升专业论文检索与问答效率。

## 2. 技术栈

- 后端语言：Python
- Agent 编排：LangGraph
- LLM 接入：OpenAI API、OceanGPT/OpenAI-compatible API
- 文档解析：Docling、PyMuPDF
- 多模态处理：PDF 图片抽取、页面截图、表格结构化、视觉摘要
- 向量检索：FAISS，缺失时降级为 numpy 向量检索
- 关键词检索：BM25
- 混合检索：Vector Retrieval + BM25 + RRF 融合
- 重排序：CrossEncoder，缺失时使用规则 Rerank
- 前端界面：Streamlit
- 数据处理：Pandas、NumPy
- 配置管理：python-dotenv
- 测试：unittest

## 3. 核心业务流程

完整流程如下：

```text
用户上传 PDF
↓
Docling / PyMuPDF 文档解析
↓
文本清洗、表格抽取、图片抽取、页面截图
↓
生成 parent chunks
↓
从 parent chunks 切出 child chunks
↓
child chunks 向量化 + BM25 建索引
↓
用户提问 + 上下文记忆
↓
LLM Router 判断是否需要外部知识库
├─ 不需要 → Direct Answer
└─ 需要 → Query Rewrite
          ↓
       多查询改写
          ↓
       Hybrid Retrieval
          ↓
       命中 child chunks
          ↓
       parent_id 回召 parent chunks
          ↓
       Rerank
          ↓
       Answer Generation
          ↓
       Source Verification
          ↓
       证据不足则二次检索
          ↓
       中文答案 + 来源页码 + 证据片段
```

## 4. 模块说明

### 4.1 PDF 解析模块

对应文件：

- `src/pdf_ingest.py`
- `src/docling_ingest.py`
- `src/multimodal.py`
- `src/text_processing.py`

系统支持两种解析后端：

```text
pymupdf：轻量、启动快，适合普通 PDF 文本解析
docling：版面理解更强，适合双栏论文、复杂表格、科研 PDF
```

Docling 后端使用：

- `DocumentConverter` 解析 PDF
- `HybridChunker` 生成上下文化 chunk
- 表格导出为 Markdown/CSV 风格文本
- 可导出 Markdown/JSON 作为中间产物

PyMuPDF 后端用于：

- 提取页面文本
- 提取图片
- 抽取表格
- 渲染页面截图

文本清洗包括：

- 去除页眉页脚
- 去除页码
- 合并断行
- 清理重复行
- 去除参考文献尾部噪声
- 保留章节标题作为 metadata

### 4.2 多模态处理模块

对应文件：

- `src/multimodal.py`

多模态入库支持：

```text
text：正文文本
table：结构化表格
image：PDF 内嵌图片
page_snapshot：整页截图
```

表格会转成 Markdown，例如：

```markdown
| Sample | Co | Ni | Mn |
| --- | --- | --- | --- |
| A1 | 0.42 | 1.23 | 18.2 |
```

图片处理逻辑：

1. 从 PDF 中抽取图片文件
2. 保存到 `data/uploads/_multimodal/`
3. 提取图题、表题、页面邻近文本
4. 如果配置视觉模型，则调用视觉模型生成图片摘要
5. 如果没有视觉模型，则使用 caption + 页面上下文生成可检索证据

这样即使没有多模态大模型，本系统也能把图题、表题、页面上下文纳入检索。

### 4.3 父子文档切片

对应文件：

- `src/parent_child.py`
- `src/vector_store.py`

父子文档切片的目标是兼顾召回精度和回答上下文完整性。

传统 RAG 问题：

```text
chunk 太小：检索准，但回答缺上下文
chunk 太大：上下文足，但向量匹配不精准
```

本项目的做法：

```text
parent chunk：较大，约 1800 字，用于回答
child chunk：较小，约 420 字，用于检索
```

检索流程：

```text
query → 检索 child chunk → 根据 parent_id 找回 parent chunk → 用 parent 回答
```

每个 `DocumentChunk` 包含：

```python
chunk_id
parent_id
chunk_role  # parent 或 child
source
page
text
modality
metadata
```

命中结果会保留：

```text
matched_child_text：实际命中的子片段
retrieved_parent_id：回召的父文档 ID
parent text：完整父文档上下文
```

### 4.4 向量索引与混合检索

对应文件：

- `src/vector_store.py`

系统实现了真正的混合检索：

```text
Vector Retrieval：语义召回
BM25 Retrieval：关键词召回
RRF Fusion：融合两路结果
Parent Recall：child 命中后回召 parent
```

向量检索：

- 优先使用 FAISS
- 如果 FAISS 不可用，降级为 numpy 点积检索
- Embedding 优先支持 OpenAI / sentence-transformers
- 如果都不可用，使用 hashing embedding 兜底，保证系统可运行

BM25 检索：

- 自实现 BM25 关键词索引
- 针对中文做 2-4 gram 切分
- 针对海洋矿产领域词做增强匹配

RRF 融合：

```text
score = 1 / (k + rank)
```

这样可以把语义相关和关键词精确命中的结果融合，减少单一路径召回失败的问题。

### 4.5 Query Rewrite 多查询改写

对应文件：

- `src/agent.py`

系统不会只用用户原问题检索，而是生成多个 query variants。

例如用户问：

```text
多金属结核成矿受哪些环境因素控制？
```

系统会扩展为：

```text
多金属结核 polymetallic nodules manganese nodules
控制因素 controlling factors formation mechanism
赋存环境 geological setting redox sedimentation
source evidence mechanism indicator distribution geochemistry mineralogy
```

多查询好处：

- 中文问题可以匹配英文论文
- 专业术语可以扩展成英文同义词
- 提高召回率
- 支持二次检索时进一步放宽查询

### 4.6 LLM Router

对应文件：

- `src/llm.py`
- `src/agent.py`

系统支持由大模型判断是否需要外部知识库。

Router 会输出 JSON：

```json
{
  "route": "rag",
  "need_retrieval": true,
  "rewritten_query": "多金属结核 控制因素 polymetallic nodules",
  "reason": "该问题需要文献证据支撑",
  "confidence": 0.91
}
```

路由逻辑：

```text
闲聊 / 系统介绍 → direct answer
专业科研问题 / 需要文献依据 / 涉及表格图件 / 追问来源 → RAG
```

如果没有配置可用 LLM，则自动降级为规则路由：

```python
if is_casual_question(question):
    direct
elif domain_term_hits(question) > 0 or len(question) >= 12:
    rag
else:
    direct
```

### 4.7 LangGraph Agent 工作流

对应文件：

- `src/agent.py`

当前节点：

```text
analyze
query_rewrite
hybrid_retrieve
rerank
generate
source_verification
direct
```

流程：

```text
analyze
├─ direct → END
└─ rag → query_rewrite
          ↓
       hybrid_retrieve
          ↓
       rerank
          ↓
       generate
          ↓
       source_verification
          ├─ 证据充分 → END
          └─ 证据不足 → query_rewrite 再检索
```

### 4.8 Rerank 重排序

对应文件：

- `src/rerank.py`

优先使用 CrossEncoder：

```text
query + chunk → CrossEncoder → relevance score
```

如果没有配置 CrossEncoder，则使用规则重排：

```text
最终分数 = hybrid score
       + query overlap
       + heading/caption overlap
       + BM25 signal
       + vector signal
       + domain boost
       + modality boost
```

其中：

```text
table/image/page_snapshot 会有轻微 modality boost
海洋矿产领域词命中会有 domain boost
标题、图题、表题命中会提高排序
```

### 4.9 Source Verification 来源校验

对应文件：

- `src/agent.py`

生成答案后系统会判断证据是否充分：

```text
是否检索到结果
最高相关性是否超过阈值
问题关键词覆盖是否足够
复杂问题是否有足够证据片段
```

如果证据不足：

```text
未达到最大检索轮次 → 回到 Query Rewrite 再检索
达到最大检索轮次 → 在答案中提示证据不足
```

这个设计可以降低幻觉风险。

### 4.10 上下文记忆

对应文件：

- `src/models.py`
- `src/agent.py`
- `app.py`

系统支持会话级上下文记忆。

记忆内容：

```text
最近 N 轮用户问题
最近 N 轮助手回答
```

用途：

```text
LLM Router 判断追问是否需要检索
Query Rewrite 处理“继续刚才的问题”
Answer Generation 理解上下文
```

例如：

```text
用户：多金属结核成矿受哪些因素控制？
助手：主要包括氧化还原条件、沉积速率、金属来源等。
用户：继续解释这些因素。
```

系统会把“继续解释这些因素”扩展为：

```text
继续解释这些因素 上下文问题: 多金属结核成矿受哪些因素控制？
```

### 4.11 OceanGPT / OpenAI-compatible API

对应文件：

- `src/llm.py`
- `src/config.py`

系统支持多种 LLM 后端：

```text
openai
ocean-gpt-api
openai-compatible
none
auto
```

你的本机 RTX 3050 4GB 显存不适合本地跑 OceanGPT 大模型，因此推荐：

```text
本机：PDF 解析、检索、rerank、source verification
远程服务器：OceanGPT 生成中文答案
```

配置示例：

```env
LLM_BACKEND=ocean-gpt-api
OCEANGPT_API_KEY=your-key
OCEANGPT_BASE_URL=https://your-server/v1
OCEANGPT_MODEL=OceanGPT
```

### 4.12 Streamlit 前端

对应文件：

- `app.py`

主要功能：

- 上传 PDF
- 选择解析后端：PyMuPDF / Docling / Auto
- 开关父子文档切片
- 配置 parent/child chunk 大小
- 配置向量召回数量
- 配置 BM25 召回数量
- 配置 Rerank top-k
- 启用多模态解析
- 启用 LLM Router
- 启用上下文记忆
- 查看 Agent trace
- 查看 query variants
- 查看 source verification 结果
- 查看命中的 child chunk 和回召的 parent chunk

## 5. 项目亮点

### 5.1 面向垂直领域

系统不是通用聊天机器人，而是面向海洋矿产科研论文，内置了多金属结核、富钴结壳、热液硫化物、稀土元素、成矿机制等领域术语扩展。

### 5.2 高质量文档解析

接入 Docling 解决普通 PDF 解析在双栏论文、表格结构、阅读顺序上的问题。

### 5.3 多模态 RAG

不仅处理正文，还能处理：

- 表格
- 图片
- 图题
- 页面截图
- 视觉摘要

### 5.4 父子文档切片

小 child 用于精确召回，大 parent 用于完整回答，有效平衡召回精度和上下文完整性。

### 5.5 混合检索

同时使用：

```text
向量语义检索
BM25 关键词检索
RRF 融合
```

比单纯向量检索更适合专业论文，因为专业术语、元素符号、样品编号、图表编号经常需要关键词精确匹配。

### 5.6 Agentic Workflow

系统不是简单 RAG，而是有：

- Router
- Query Rewrite
- Retrieval
- Rerank
- Generation
- Source Verification
- 二次检索
- Memory

### 5.7 中文回答

即使上传英文论文，最终答案也会用中文表达，原文证据保留用于溯源。

## 6. 简历写法

### 版本一：简洁版

> 基于 Python、LangGraph、Docling、FAISS、BM25、Streamlit 构建海洋矿产科研文献 Agentic RAG 问答系统，支持 PDF 文本/表格/图片解析、父子文档切片、混合检索、Rerank、LLM Router、上下文记忆和来源追溯，实现对中英文科研论文的中文问答。

### 版本二：技术细节版

> 负责设计并实现海洋矿产科研文献智能问答系统：使用 Docling/PyMuPDF 完成 PDF 版面解析和多模态图表抽取；设计 parent-child chunking 策略，以 child chunk 进行向量/BM25 混合召回，并回召 parent chunk 保证回答上下文完整；基于 LangGraph 构建 Agentic RAG 工作流，包含 LLM Router、Query Rewrite、Hybrid Retrieval、Rerank、Source Verification 和二次检索；支持 OpenAI/OceanGPT-compatible API、上下文记忆和 Streamlit 可视化问答界面。

### 版本三：成果导向版

> 构建面向海洋矿产论文的 Agentic RAG 系统，将传统 PDF 阅读流程转化为可追溯的中文智能问答流程。系统支持中英文论文解析、图表证据入库、父子文档切片、混合检索和来源校验，降低专业文献检索成本，并通过评测脚本支持对检索命中率和证据覆盖率进行量化评估。

## 7. 面试高频问题与参考答案

### Q1：你这个项目解决了什么问题？

答：

这个项目解决的是海洋矿产科研论文阅读和知识检索效率低的问题。传统方式需要人工翻阅大量 PDF，尤其是英文论文、双栏排版、表格和图片信息很难快速定位。我的系统可以自动解析论文，把正文、表格、图片、页面截图都转成可检索证据，然后通过 Agentic RAG 根据用户问题检索相关片段，生成带来源页码的中文回答。

### Q2：为什么不用普通 ChatGPT 直接问？

答：

普通大模型没有用户上传论文的完整上下文，容易产生幻觉，也无法保证回答来自哪篇论文、哪一页。我的系统使用 RAG 先检索论文证据，再基于证据生成答案，并且会返回 source、页码和原文片段，适合科研场景下需要可追溯性的问答。

### Q3：为什么选择 Agentic RAG，而不是普通 RAG？

答：

普通 RAG 通常是固定流程：检索一次，然后生成答案。这个项目的问题更复杂，需要判断是否需要检索、是否要改写查询、证据是否足够、是否要二次检索。因此我用 LangGraph 做 Agentic Workflow，把流程拆成 Router、Query Rewrite、Hybrid Retrieval、Rerank、Generation、Source Verification 等节点，让系统能够根据问题和证据状态自适应执行。

### Q4：你的 Router 是怎么做的？

答：

Router 优先由大模型判断是否需要调用外部知识库，要求模型输出严格 JSON，包括 route、need_retrieval、rewritten_query、reason 和 confidence。如果没有配置可用的大模型，则降级为规则路由，根据问题是否是闲聊、是否命中海洋矿产领域词、是否像上下文追问来判断。

### Q5：什么问题会走 Direct Answer？

答：

比如“你能做什么”“怎么使用系统”“你好”等不需要论文证据的问题会走 Direct Answer。如果问题涉及论文依据、专业结论、表格、图件、source、页码、具体数值或海洋矿产专业知识，一般会走 RAG 检索。

### Q6：为什么要做 Query Rewrite？

答：

因为用户通常用中文提问，但论文可能是英文。比如用户问“多金属结核成矿受哪些因素控制”，英文论文中可能写的是 “polymetallic nodules”、“controlling factors”、“redox condition”、“sedimentation rate”。Query Rewrite 可以把中文问题扩展为中英文混合查询，提高召回率。

### Q7：什么是父子文档切片？

答：

父子文档切片是指入库时先生成较大的 parent chunk，再从 parent 中切出较小的 child chunk。检索时只检索 child chunk，因为小片段匹配更精准；命中 child 后通过 parent_id 回召完整 parent chunk，用 parent 上下文生成答案。这样既保证检索精度，也避免回答时上下文不足。

### Q8：为什么不直接用大 chunk？

答：

大 chunk 虽然上下文完整，但向量语义会被稀释，检索不够精准。比如一个 2000 字 chunk 里只有一小段讲 Ce 异常，用户问 Ce 异常时，这个大 chunk 的向量可能不够靠前。child chunk 可以精准命中关键句，再回召 parent 保证上下文完整。

### Q9：为什么不直接用小 chunk？

答：

小 chunk 检索准，但回答容易断章取义。科研论文的结论往往依赖前后文、图题、表格说明或章节标题。只用小 chunk 可能缺少背景。因此我用 child 检索、parent 回召。

### Q10：你的混合检索是怎么实现的？

答：

系统同时做向量检索和 BM25 关键词检索。向量检索负责语义相关，BM25 负责专业词、元素符号、样品编号、图表编号等精确匹配。两路结果用 RRF 融合，最后再进入 parent recall 和 rerank。

### Q11：为什么需要 BM25？向量检索不够吗？

答：

在科研论文里很多查询依赖精确关键词，比如 “Ce anomaly”、“Y/Ho”、“Co”、“Ni”、“Table 2”、“sample A1”。向量检索对语义相似更好，但对这些符号和编号未必稳定。BM25 可以补足关键词精确匹配能力。

### Q12：RRF 是什么？

答：

RRF 是 Reciprocal Rank Fusion，用排名倒数融合多个检索结果。公式类似：

```text
score = 1 / (k + rank)
```

它不依赖不同检索器的原始分数尺度，适合融合向量检索和 BM25 这种分数分布不同的结果。

### Q13：Rerank 怎么做？

答：

如果配置 CrossEncoder，就用 CrossEncoder 对 query 和候选片段重新打分。如果没有模型，就使用规则 rerank，综合 hybrid score、query overlap、标题/图题命中、BM25 signal、vector signal、领域词加权和多模态类型加权。

### Q14：为什么需要 Rerank？

答：

第一阶段检索更关注召回率，会召回一些相关但不够精准的片段。Rerank 可以在候选结果中重新排序，把最能回答问题的证据排到前面，从而提高最终回答准确率。

### Q15：Source Verification 是什么？

答：

Source Verification 是答案生成后的证据校验节点。它会检查是否有检索结果、最高相关性是否达标、问题关键词覆盖是否足够、复杂问题是否有足够证据。如果证据不足，会触发二次检索或在答案中提示证据不足，避免模型硬答。

### Q16：你的系统怎么降低幻觉？

答：

主要有四层：

1. 回答只基于检索到的文献片段
2. 返回 source、页码和原始证据
3. Source Verification 检查证据充分性
4. 证据不足时触发二次检索或提示不足

### Q17：Docling 在项目中起什么作用？

答：

Docling 用于增强 PDF 文档解析。相比直接用 PyMuPDF 抽文本，Docling 对科研 PDF 的版面结构、阅读顺序、表格结构更友好，还能配合 HybridChunker 生成上下文化 chunk。项目中可以选择 Docling 或 PyMuPDF 作为解析后端。

### Q18：为什么还保留 PyMuPDF？

答：

PyMuPDF 更轻量、启动快、依赖少，适合普通 PDF 和快速解析。Docling 更强但依赖更重，首次运行可能较慢。因此项目保留两个后端，兼顾稳定性和解析质量。

### Q19：多模态是怎么做的？

答：

系统从 PDF 中抽取表格、图片和页面截图。表格会转成 Markdown 文本入库；图片会保存到本地，并结合图题、页面上下文生成视觉证据。如果配置视觉模型，还可以调用视觉模型生成图片摘要。最终这些证据和正文一样进入检索索引。

### Q20：如果论文是英文，为什么能中文回答？

答：

系统在 Query Rewrite 阶段会生成中英文混合检索词，能够召回英文论文片段。在 Answer Generation 阶段，提示词强制要求最终答案用中文表达。没有 LLM 时，兜底逻辑也会把英文证据概括成中文摘要。

### Q21：上下文记忆是怎么实现的？

答：

Streamlit session 中保存最近几轮用户问题和助手回答，传给 Router、Query Rewrite 和 Answer Generation。这样用户问“继续解释这些因素”时，系统能结合上一轮问题理解“这些因素”指什么。

### Q22：上下文记忆是长期记忆吗？

答：

不是长期记忆，目前是会话级短期记忆，保存在 Streamlit session_state 中。它适合多轮对话，但刷新或清空后会丢失。如果要做长期记忆，可以把对话历史存到数据库或向量库。

### Q23：项目里有没有 Tool Calling？

答：

目前不是 OpenAI 标准 function calling。系统有工具式节点，比如检索、rerank、source verification，但这些是 LangGraph 固定编排的节点，不是由模型自由选择 tool。当前已经实现 LLM Router 选择是否走 RAG，但还没有让模型自主调用多个工具函数。

### Q24：OceanGPT 在项目里怎么用？

答：

由于本机显存只有 4GB，不适合本地跑 OceanGPT 大模型，所以项目支持 OceanGPT 的 OpenAI-compatible API 接入。也就是本机负责解析、检索、rerank 和证据校验，远程 OceanGPT 负责生成中文答案。

### Q25：为什么你的电脑不适合本地跑 OceanGPT？

答：

OceanGPT 这类大模型通常需要较大的 GPU 显存，尤其多模态模型可能需要 24GB 以上显存。我这台电脑是 RTX 3050 Laptop，只有 4GB 显存，适合跑轻量检索和前端，不适合本地部署大模型。因此采用远程 API 架构更合理。

### Q26：如果面试官问准确率有多少，你怎么回答？

答：

我不会直接声称固定准确率，因为 RAG 准确率依赖文档质量、问题集和评测标准。我在项目中加入了评测脚本，可以用标注问题集计算 source hit rate、term recall、verification pass rate 等指标。工程上通过 Docling 解析、父子切片、混合检索、rerank 和 source verification 提升准确率。

### Q27：你怎么评估 RAG 系统？

答：

可以从三个层面评估：

1. 检索层：Hit@K、MRR、source hit rate
2. 证据层：关键词覆盖率、证据是否来自正确页码
3. 答案层：答案是否被证据支持、是否有幻觉、中文表达是否准确

项目中有 `tools/evaluate.py`，可以读取 JSONL 评测集进行自动评测。

### Q28：如果检索不到正确答案怎么办？

答：

系统会先通过 Source Verification 判断证据不足，然后进行二次 Query Rewrite 和再检索。如果仍然不足，就在答案中明确说明证据不足，并建议上传更多论文或缩小问题范围。

### Q29：这个项目最大的技术难点是什么？

答：

我认为最大的难点是让科研 PDF 变成高质量、可追溯、可检索的知识单元。因为科研 PDF 有双栏排版、表格、图件、英文术语、跨页信息等问题。为了解决这个问题，我接入 Docling、多模态抽取、父子文档切片和混合检索，而不是简单按固定长度切文本。

### Q30：如果让你继续优化，你会怎么做？

答：

我会从几个方向继续优化：

1. 引入真正的 function calling，让模型自主选择 retrieve、inspect table、verify source 等工具
2. 加 Human-in-the-loop，让用户确认证据是否足够
3. 做长期记忆，把用户偏好和常用论文集持久化
4. 引入更强的领域 embedding 和 reranker
5. 对图件做更强的视觉理解，比如接远程 OceanGPT-o 或 GPT-4o vision
6. 构建更完整的评测集，量化检索和问答准确率

## 8. 面试时可以强调的取舍

### 8.1 为什么不用纯大模型？

因为科研场景要求来源可追溯，纯大模型容易幻觉。

### 8.2 为什么不用纯向量检索？

因为专业论文里有大量精确术语、元素符号、样品编号，BM25 很有价值。

### 8.3 为什么做父子切片？

为了同时保证检索精度和回答上下文完整。

### 8.4 为什么保留降级路径？

实际项目中依赖可能缺失，API 可能不可用。降级路径可以保证系统在没有 LLM、没有 FAISS、没有 CrossEncoder 时仍能运行，体现工程健壮性。

### 8.5 为什么用 Streamlit？

Streamlit 适合快速搭建 AI 原型界面，方便上传 PDF、调参、查看检索结果和 Agent trace。

## 9. 项目启动方式

```powershell
cd C:\Users\76585\Documents\Codex\2026-07-05\ne\outputs\mining-agentic-rag
.\run.ps1
```

打开：

```text
http://localhost:8501
```

如果使用 Docling：

```powershell
.\install_docling.ps1
```

如果使用高准确率依赖：

```powershell
.\install_accuracy_deps.ps1
```

## 10. 面试一句话总结

这个项目不是简单调用大模型问答，而是完整实现了一个面向海洋矿产科研文献的 Agentic RAG 系统：从复杂 PDF 解析、多模态证据入库、父子文档切片、混合检索、Rerank、LLM Router、上下文记忆，到来源校验和中文答案生成，重点解决科研问答中的准确性、可追溯性和专业术语召回问题。
